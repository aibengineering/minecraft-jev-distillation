/** Vanilla 1.21.4 facts the decision loop relies on. */

/** A raised shield blocks nothing until it has been up this many ticks. */
export const SHIELD_READY_TICKS = 5;

/** Vanilla ProjectileUtil expands the target's box by this much on every face when testing a projectile hit. */
export const PROJECTILE_HIT_MARGIN = 0.3;
